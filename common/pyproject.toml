[project]
name = "openscripts"
version = "0.1.2"
description = ""
authors = [{ name = "Kraktun" }]
requires-python = ">=3.12,<4"
readme = "README.md"

dependencies = [
    "click>=8.4.2",
    "emoji>=2.15.0",
    "mutagen>=1.48.1",
    "pillow>=12.3.0",
    "psutil>=7.2.2",
    "pypiwin32>=223",
    "pyyaml>=6.0.3",
]

[tool.uv]

[tool.uv.build-backend]
module-root = ""

[build-system]
requires = ["uv_build>=0.9.25,<0.10.0"]
build-backend = "uv_build"
